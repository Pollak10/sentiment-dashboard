# Server
server.port=8080

# PostgreSQL - Render Database
spring.datasource.url=jdbc:postgresql://dpg-d7br5mn5r7bs73duap0g-a:5432/sentiment_dashboard_c6u9
spring.datasource.username=sentiment_dashboard_c6u9_user
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Disable Kafka in production
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# API Keys (set as environment variables on Render)
api.huggingface.key=${HUGGINGFACE_KEY}
api.news.key=${NEWS_KEY}
api.finnhub.key=${FINNHUB_KEY}
api.coingecko.key=${COINGECKO_KEY}